{
  "schemaVersion": 3,
  "task": "Restructure the Pro Rust web shell so the root page is an awesome-design-md xAI-inspired dialogue-only homepage, with graph/source/gate/review controls moved to a separate /graph page that renders the knowledge graph as a cinematic galaxy/star map. Reuse existing Pro API/client patterns, update DESIGN.md, docs/PROJECT_STATE.md, docs/pro-rust-architecture.md, and .agent-guardrails evidence, and do not change OSS runtime or add credentials/provider execution.",
  "preset": "generic",
  "createdAt": "2026-04-25T16:53:01.207Z",
  "allowedPaths": [
    "DESIGN.md",
    "pro/apps/web/",
    "docs/pro-rust-architecture.md",
    "docs/PROJECT_STATE.md",
    ".agent-guardrails/"
  ],
  "requiredCommands": [
    "cargo test --manifest-path pro/Cargo.toml"
  ],
  "evidencePaths": [
    ".agent-guardrails/evidence/current-task.md"
  ],
  "intendedFiles": [],
  "protectedPaths": [],
  "allowedChangeTypes": [
    "implementation",
    "interface",
    "tests",
    "docs",
    "guardrails-internal",
    "other"
  ],
  "riskLevel": "standard",
  "requiresReviewNotes": false,
  "validationProfile": "standard",
  "securityRequirements": [
    "Do not add credential fields, provider key entry, auth token handling, provider execution, worker execution, or quota/billing mutation.",
    "Keep all existing Pro execution/admission/intent panels preview-only and rejected where the backend says they are rejected."
  ],
  "dependencyRequirements": [
    "Do not add Rust, npm, Python, image, icon, font, or external asset dependencies for this UI redesign."
  ],
  "performanceRequirements": [
    "Avoid heavy animation loops, large blur surfaces, remote assets, and client frameworks; keep the server-rendered Maud web shell lightweight."
  ],
  "understandingRequirements": [
    "Explain that this is a visual/interaction redesign of the Pro shell only, not a change to hosted execution capability."
  ],
  "continuityRequirements": [
    "Reuse the existing Maud render helpers, browser fetchJson pattern, API endpoints, graph selection behavior, and preview-only execution panels."
  ],
  "acknowledgedSkips": [],
  "patternSummary": "Pro web is a server-rendered Rust/Maud shell with CSS and a small inline script. Keep API and domain payloads unchanged; split the visual surface into a dialogue-only root and a separate graph workspace while preserving graph/actions on /graph.",
  "smallestViableChange": "Add a dialogue-only / homepage and move the existing star-map graph workspace to /graph with xAI-inspired monochrome styling and low-cost cinematic motion while preserving all existing endpoints/actions.",
  "assumptions": [
    "The active task is Pro frontend redesign only.",
    "DESIGN.md plus the awesome-design-md xAI reference are the local design source: stark black, spectral white, mono/Universal Sans feel, uppercase telemetry, ghost controls, and cinematic full-field composition; the UI must not imply affiliation with any referenced brand.",
    "The knowledge graph remains the primary workspace, but no longer has to be the root homepage."
  ],
  "acceptanceCriteria": [
    "The Pro web shell root page renders only the causal dialogue entry and does not render provider, source, queue, inspector, evidence, review, or gate panels.",
    "The /graph page still renders every existing graph, run, provider, queue, admission, intent, storage, vault, quota, event, review, and commit panel/action expected by current tests.",
    "The /graph page makes the graph feel like a cinematic galaxy/star map and the root page uses the high-trust monochrome xAI-inspired style from DESIGN.md.",
    "No OSS runtime files, provider credential flows, dependency files, or API/domain behavior are changed.",
    "Docs and evidence describe the redesign and remaining capability boundaries."
  ],
  "nonGoals": [
    "No OSS runtime changes",
    "No new API endpoints",
    "No provider calls",
    "No credential/key fields",
    "No auth/session/JWT implementation",
    "No worker execution",
    "No quota or billing mutation",
    "No new dependencies",
    "No package publishing"
  ],
  "expectedBehaviorChanges": [
    "The Pro browser shell root page becomes a dialogue-only entry surface, while the /graph page becomes the cinematic galaxy/star-map workspace."
  ],
  "userVisibleEffects": [
    "The current Pro homepage appears as a premium dialogue box rather than a dense workspace, and the graph/source/gate/review controls live on the /graph workspace."
  ],
  "intendedSymbols": [],
  "expectedPublicSurfaceChanges": [
    "Pro web root page visual structure and styling"
  ],
  "expectedBoundaryExceptions": [],
  "expectedTestTargets": [
    "cargo test --manifest-path pro/Cargo.toml",
    "cargo build --manifest-path pro/Cargo.toml",
    "browser smoke of http://127.0.0.1:3007/",
    "browser smoke of http://127.0.0.1:3007/graph"
  ],
  "productionProfile": "",
  "nfrRequirements": [
    "Maintain readable contrast, keyboard-accessible controls, compact CSS, no remote assets, no secret-shaped strings, and responsive fallback below 1080px."
  ],
  "expectedLoadSensitivePaths": [],
  "expectedConcurrencyImpact": "None. This is a server-rendered UI/CSS/script refactor with no backend concurrency changes.",
  "observabilityRequirements": [],
  "rollbackNotes": "Revert the Pro web-shell commit; no migration or dependency rollback is required.",
  "riskJustification": "The redesign is confined to the Pro Rust web shell plus docs/evidence. Backend API behavior, storage, credentials, queues, and OSS runtime remain unchanged.",
  "guardRules": [
    "添加相应的测试",
    "遵循现有代码风格",
    "保持向后兼容",
    "更新 API 文档"
  ],
  "roughIntent": {
    "detected": true,
    "taskType": "feature",
    "confidence": 0.65,
    "sources": {
      "taskType": "关键词匹配 (65% 置信度)",
      "paths": "关键词+文件树",
      "testCommand": "package.json",
      "riskLevel": "基于任务类型: feature"
    }
  },
  "autoFilledFields": [
    "allowedPaths",
    "requiredCommands",
    "riskLevel",
    "guardRules"
  ]
}

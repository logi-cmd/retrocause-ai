# RetroCause State Pointer

This file used to contain a dated scratch log. It drifted and contained old
provider/key notes, so the maintained source of truth is now:

- `docs/PROJECT_STATE.md` for current status and next work
- `docs/INDEX.md` for the documentation map
- `.agent-guardrails/evidence/current-task.md` for task-level evidence

Current boundary: OSS is a keyless local alpha. OpenRouter is deprecated and is
not part of the supported OSS browser/API surface. Pro work continues as a
separate Rust full-stack line.

{
  "scripts": {
    "test": "npm --prefix frontend run lint && npm --prefix frontend run build && python -m ruff check retrocause/ && python -m pytest tests/ --basetemp=.tmp-tests/pytest && python scripts/e2e_test.py"
  },
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
